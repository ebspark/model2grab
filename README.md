have a obj.
run remesh-iso.py like so in the terminal python remesh-iso.py input.obj output.obj --length 0.04 
swap input obj with your obj.
Go into blender import output.obj
select the model and run blenderscript-iso.py file.
delete the orginal output.obj model and default triangle.
then select all triangles and run the model2grab.py script in bllender too! 
use index0tools to convert the json into a level file.
